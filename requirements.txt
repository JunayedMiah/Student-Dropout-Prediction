# Core data handling
pandas==2.3.3
numpy==2.4.4

# Distributed data processing (Databricks provides this on cluster,
# only needed here for local development/testing outside Databricks)
pyspark==4.2.0

# Deep learning
# CPU only machine: install with
#   pip install torch==2.13.0 --index-url https://download.pytorch.org/whl/cpu
# to skip the large CUDA dependency set. On a GPU cluster, the line below
# pulls the CUDA enabled build automatically.
torch==2.13.0

# Hyperparameter tuning
optuna==4.9.0

# Experiment tracking and model registry
mlflow==3.15.1

# Visualization
matplotlib==3.10.8
seaborn==0.13.2
